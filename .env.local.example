NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
ARCHIVE_PASSWORD=
# Optional: Iframely API key (free tier 10K/mo). When set, Instagram
# imports use Iframely first to get full-resolution carousel slides
# and original aspect ratio. Without it the import falls back to
# best-effort scraping which Instagram increasingly blocks from
# cloud IPs.
IFRAMELY_KEY=
